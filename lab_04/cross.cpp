#include <iostream>
#include <string>

void cross(int size){
  int count;
  if(size % 2 == 0){
    for(count == 0; count < size; count++){
      
    }
  }
}
int main(){
  std::cout<<"Input size: ";
  int size;
  std::cin>>size;
  std::cout<<"\n";
  std::cout<<"Shape: ";
  std::cout<<"\n";
  cross(size);
}
